fastapi==0.119.0
uvicorn[standard]>=0.21.1,<0.38.0

python-dotenv==1.0.0
pydantic==2.7.0

# LangChain core and community integrations (imports use langchain and langchain_community)
langchain>=0.3.0,<1.0
langchain-community==0.4

# Hugging Face embeddings integration (import: from langchain_huggingface import HuggingFaceEmbeddings)
langchain-huggingface>=0.0.3

# Sentence Transformers (embedding model backend required by HuggingFaceEmbeddings)
sentence-transformers>=2.2.2,<5.0.0

# FAISS vector store (binary wheels are platform dependent; see notes below)
faiss-cpu>=1.7.2

# PDF loader used by langchain-community's PyPDFLoader (uses pypdf)
pypdf>=3.8.0

# Google GenAI SDK (Gemini) - used as `from google import genai`
google-genai>=0.0.1

# Google Custom Search (google-api-python-client
google-api-python-client>=2.70.0
